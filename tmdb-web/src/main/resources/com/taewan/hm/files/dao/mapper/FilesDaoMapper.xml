<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.taewan.hm.files.dao.FilesDao">
    <insert id="insertPosterImageFile"
            parameterType="com.taewan.hm.files.vo.request.PosterVO">
            INSERT INTO FILES
             (FILE_NUM
            , FILE_GROUP_ID
            , OBFUSCATE_NAME
            , DISPLAY_NAME
            , EXTEND_NAME
            , FILE_LENGTH
            , FILE_PATH)
            VALUES
            ( (SELECT NVL(MAX(FILE_NUM), 0) + 1
                 FROM FILES
                WHERE FILE_GROUP_ID = #{fileGroupId})
            , #{fileGroupId}
            , #{obfuscateName}
            , #{displayName}
            , #{extendName}
            , #{fileLength}
            , #{filePath})
    </insert>
    
    <select id="selectPosterImgByMovieId"
             parameterType="string"
             resultType="com.taewan.hm.files.vo.response.LoadPosterVO">
             SELECT DISPLAY_NAME
                  , EXTEND_NAME
                  , FILE_LENGTH
                  , FILE_PATH
               FROM FILES
              WHERE FILE_GROUP_ID = #{_parameter}
    </select>
    
    <select id="selectFilePathByFileGroupId"
            parameterType="string"
            resultType="string">
     SELECT FILE_PATH
       FROM FILES
      WHERE FILE_GROUP_ID = #{fileGroupId}
    </select>
    
    <delete id="deleteFileByFileGroupId"
            parameterType="string">
     DELETE
       FROM FILES
      WHERE FILE_GROUP_ID = #{fileGroupId}
    </delete>
            
</mapper>