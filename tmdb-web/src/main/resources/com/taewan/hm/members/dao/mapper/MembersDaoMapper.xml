<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.taewan.hm.members.dao.MembersDao">

    <insert id ="insertNewMember"
            parameterType="com.taewan.hm.members.vo.request.RegistVO">
	  INSERT INTO MEMBERS
	         (EMAIL
	        , NAME
	        , PASSWORD
	        , SALT
	        , REGIST_DATE
	        , LATEST_PASSWORD_CHANGE_DATE
	        , LOGIN_FAIL_COUNT)
	        VALUES
	         (#{email}
	        , #{name}
	        , #{password}
	        , #{salt}
	        , SYSDATE
	        , SYSDATE
	        , 0)
    </insert>

    <select id="selectMemberByUserEmail"
            parameterType="string"
            resultType="com.taewan.hm.members.vo.MembersVO">
        SELECT EMAIL
             , NAME
             , PASSWORD
          FROM MEMBERS
         WHERE EMAIL = #{_parameter}
    </select>
</mapper>